{
  "name": "critterbox",
  "private": true,
  "version": "0.9.0",
  "type": "module",
  "description": "Browser-based 3D animal-ecosystem simulation — Polish fauna on a procedural heightmap world.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "sim-check": "node scripts/sim-check.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
